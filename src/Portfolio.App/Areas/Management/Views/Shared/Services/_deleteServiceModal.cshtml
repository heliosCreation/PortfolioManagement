@model ServiceModel


<form asp-controller="Services" asp-action="Delete" asp-route-id="@Model.Id">
    <div class="modal-header">
        <span class="fire-container inner-shadow">
            <i class="fab fa-dev fire-icon"></i>
        </span>
        <h5 class="modal-title">
            Delete your skill
        </h5>

        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
            <span aria-hidden="true">&times;</span>
        </button>
    </div>
    <div class="modal-body">
        <div class="form-group">
            <div class="row">
                <p>Are you sure you  want to delete your <span class="important-text">@Model.Name</span> type of service ?</p>
            </div>
        </div>
    </div>
    <div class="modal-footer">
        <button type="submit" class="btn btn-outline-success">Delete</button>
    </div>

</form>